{{- if .Values.rbac.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "atlassian-hub.fullname" . }}
  labels:
    {{- include "atlassian-hub.labels" . | nindent 4 }}
rules: []
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "atlassian-hub.fullname" . }}
  labels:
    {{- include "atlassian-hub.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "atlassian-hub.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "atlassian-hub.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
